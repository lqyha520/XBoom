# 复制为 update-mirror.env 后填写（不要提交含密码的 .env 到 Git）

# 国内镜像对外访问地址（必须 HTTPS，末尾不要斜杠）
# 示例: https://update.yourdomain.com/aiwritex
MIRROR_BASE_URL=

# SSH 上传（二选一：填 SSH 则自动 scp，不填则只生成本地文件）
SSH_HOST=
SSH_USER=root
SSH_PORT=22
# 服务器上的目录，需能被 Nginx/静态站点访问
REMOTE_DIR=/var/www/aiwritex-updates
